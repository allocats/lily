module foo;

import std::io;

enum Kind {
    Expr;
    Binop;
}

union Nodes {
    expr: AstExpr; 
    binop: AstBinop;
}

struct AstNodes {
    kind: Kind;
    as: Nodes;
}

fn main(argc: i32, argv: i8**) {
    std::io::println("Hello world! :3");
}
