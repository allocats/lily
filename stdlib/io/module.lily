#include "libc_stdio.lily";

println :: fn(fmt: *u8, args: ...) -> i32 {
    x: i32 = vprintf(fmt, args);
    putchar(10);
    return x + 1;
}

print :: fn(fmt: *u8, args: ...) -> i32 {
    return vprintf(fmt, args);
}
